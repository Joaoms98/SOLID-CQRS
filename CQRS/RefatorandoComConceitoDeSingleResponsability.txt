Foi refatorado nos controladores, códigos que se repetiam.

Como: 
BuscarLeilaoPorId
BuscarTodosLeiloes
e outros


Foi reduzido uma linha de código, para cada metodo do controlador criando o arquivo LeilaoDao dentro de "Dados"
e no controlador você instacia com: 

        AppDbContext _context;
        LeilaoDao _dao; <<<<<

        public LeilaoApiController()
        {
            _context = new AppDbContext();
            _dao = new LeilaoDao();   <<<<<<
        }


para poder usa-lo em: 

        [HttpGet("{id}")]
        public IActionResult EndpointGetLeilaoById(int id)
        {
            var leilao = _dao.BuscarLeilaoPorId(id);<<<<< por exemplo
            if (leilao == null) return NotFound();
            return Ok(leilao);
        }